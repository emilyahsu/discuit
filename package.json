{
  "name": "discuit",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd ui && npm install && npm install -g typescript && npm run build && cd .."
  }
} 