{
  "name": "discuit",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postinstall": "cd ui && npm ci && npm install -g typescript && npm run build && cd .."
  }
} 