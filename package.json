{
  "name": "discuit",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-build": "tsc",
    "heroku-postbuild": "cd ui && npm ci && npm run build && cd .."
  }
} 