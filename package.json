{
  "name": "discuit",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postinstall": "npm install -g typescript && cd ui && npm ci && npm run build && cd .."
  }
} 